kind: Module
type: container
name: dashboard
hotReload:
  sync:
  - target: /app
services:
  - name: dash
    ports:
      - name: http
        containerPort: 8501
    dependencies:
      - generate-synthetic-data
