kind: Module
type: container
name: dashboard
disabled: false

services:
  - name: dash
    ports:
      - name: http
        containerPort: 8501
    # dependencies:
    #   - generate-synthetic-data
    devMode:
      sync:
        - target: /app
          source: src
          mode: two-way
